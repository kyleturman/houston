# Apple Developer Team ID
# Find at: https://developer.apple.com/account → Membership Details
APPLE_TEAM_ID=YOUR_TEAM_ID

# App Store Connect API Key
# Generate at: App Store Connect → Users and Access → Integrations → App Store Connect API
# 1. Click "+" to generate a new key with "App Manager" role
# 2. Download the .p8 file (you can only download it ONCE)
# 3. Note the Key ID and Issuer ID shown on the page

ASC_KEY_ID=YOUR_KEY_ID
ASC_ISSUER_ID=YOUR_ISSUER_ID
ASC_KEY_PATH=AuthKey.p8

# Note: Contact info and other settings are in ios/config/testflight.yml
